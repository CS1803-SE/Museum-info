<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.museuminfo">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:hardwareAccelerated="false"
        android:largeHeap="true"
        android:theme="@style/Theme.MuseumInfo">
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".LoginActivity"/>
        <activity android:name=".MainActivity"/>
        <activity android:name=".ShowMuseumActivity"/>
        <activity android:name=".SearchActivity"/>
        <activity android:name=".ShowExhibition"/>
        <activity android:name=".ShowCangpin"/>
        <activity android:name=".ShowCangpin_detail"/>
        <activity android:name=".ShowNews"/>
        <activity android:name=".ShowNews_detail"/>
        <activity android:name=".ShowExhi_detail"/>
        <activity android:name=".DianPingActivity"/>
    </application>

</manifest>